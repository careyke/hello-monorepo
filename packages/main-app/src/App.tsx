import React from "react";
import { hot } from "react-hot-loader";
import { FlexRow } from "@carey/react-base-components";

const App = () => {
  return <FlexRow>hello monorepo</FlexRow>;
};

export default hot(module)(App);
